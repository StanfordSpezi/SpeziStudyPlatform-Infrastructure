apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../base
  - cert-issuer.yaml
  - tls-certificate.yaml
  - network-policies.yaml
  - keycloak-config-cli-job.yaml
  - pdb.yaml
patches:
  - path: patches/db-cluster.yaml
  - path: patches/keycloak-db-cluster.yaml
  - path: patches/vault-token-secret.yaml
  - path: patches/keycloak.yaml
  - path: patches/keycloak-realm-url-patch.yaml
  - path: patches/main-ingress.yaml
  - path: patches/auth-ingress.yaml
